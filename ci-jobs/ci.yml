jobs:
  - job: adbCI
    pool:
      vmImage: macOS-10.13
    variables:
      CI: true
      TERM: dumb
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: ">=10 <10.16 || >10.16"
    - script: npm install
      displayName: Install Node dependencies
    - script: npm test
      displayName: NPM Test
